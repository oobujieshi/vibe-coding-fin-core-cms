# 财务管理模块开发方案

> 基于《财务管理模块开发需求文档》V1.1 分析生成  
> 生成日期：2026-06-14

---

## 目录

1. [需求分析摘要](#1-需求分析摘要)
2. [系统架构设计](#2-系统架构设计)
3. [技术选型明细](#3-技术选型明细)
4. [模块拆分与职责](#4-模块拆分与职责)
5. [数据库设计](#5-数据库设计)
6. [API 接口设计](#6-api-接口设计)
7. [安全方案设计](#7-安全方案设计)
8. [高并发与性能优化](#8-高并发与性能优化)
9. [开发路线图](#9-开发路线图)
10. [风险评估与应对](#10-风险评估与应对)
11. [验收标准](#11-验收标准)

---

## 1. 需求分析摘要

### 1.1 项目背景

为公司业务订单结算与资金管理开发一套财务管理模块，覆盖从订单结算、收付款管理、资金流水追踪到报表分析的全流程，并支持 PC 端与微信小程序端双终端操作。

### 1.2 核心功能模块

| 模块 | 功能要点 | 优先级 |
|------|----------|--------|
| **业务订单结算管理** | 订单CRUD、状态流转（待结算/已结算/已关闭）、批量计费（合同条款/阶梯费率）、结算单据生成（付款单/收款单）、Excel导入导出及数据校验 | P0 |
| **收款与付款管理** | 收款记录与扫码核销、多级审批付款流程、客户账单（合并/拆分/多种渠道发送） | P0 |
| **资金账号与流水管理** | 银行账户维护、流水实时同步（手动+银行接口）、自动流水核对与异常标记 | P1 |
| **基础报表** | 收支明细报表、资金余额报表、结算统计报表，均支持 Excel/PDF 导出 | P0 |
| **小程序端功能** | 账单查询与支付确认、扫码核销、审批通知、报表摘要查看 | P1 |

### 1.3 非功能需求

| 类别 | 要求 |
|------|------|
| **性能** | 支持批量结算高并发场景，数据查询响应时间 ≤ 3 秒 |
| **安全** | 敏感信息加密存储与传输，RBAC 三级权限，全操作审计日志 |
| **合规** | 满足国家财务法规要求（数据留存、审批留痕） |
| **可用性** | 界面简洁直观，操作符合财务人员习惯，支持 PC + 小程序 |
| **可维护性** | 代码结构清晰，关键逻辑模块化，提供完整开发文档 |

### 1.4 技术栈约束（需求文档指定）

| 层次 | 技术 |
|------|------|
| 后端语言 | Java |
| 后端框架 | Spring Boot |
| 数据库 | MySQL / PostgreSQL |
| 缓存 | Redis（可选） |
| PC 前端 | Vue.js / React + Element UI |
| 小程序端 | 微信小程序框架 + 微信支付 API |
| 接口规范 | RESTful API，JSON 格式 |
| 部署环境 | 云服务器（阿里云/腾讯云）或本地部署 |

### 1.5 参考产品借鉴要点

- **金蝶 KIS/K3**：模块化设计、流程自动化、多维度报表体系
- **用友 U8/U9**：资金管理、应收应付联动、合规性强

---

## 2. 系统架构设计

### 2.1 整体架构

采用 **分层微服务架构**，遵循 DDD（领域驱动设计）思想进行模块划分。

```
┌─────────────────────────────────────────────────────────┐
│                      前端层                              │
│   ┌──────────────────┐    ┌──────────────────┐          │
│   │  PC Web          │    │  微信小程序       │          │
│   │  Vue 3 +         │    │  原生框架 +       │          │
│   │  Element Plus    │    │  微信支付 API     │          │
│   └────────┬─────────┘    └────────┬─────────┘          │
├────────────┼───────────────────────┼────────────────────┤
│            │      网关层           │                    │
│   ┌────────┴───────────────────────┴─────────┐         │
│   │       API Gateway (Spring Cloud Gateway)  │         │
│   │       统一鉴权 / 限流 / 路由 / 日志       │         │
│   └────────┬───────────────────────┬─────────┘         │
├────────────┼───────────────────────┼────────────────────┤
│            │      业务服务层        │                    │
│   ┌────────┴──────┐ ┌──────────────┴─┐                 │
│   │ order-service │ │ payment-service │                 │
│   │ (订单结算服务) │ │ (收付款服务)    │                 │
│   └────────┬──────┘ └────────┬───────┘                 │
│   ┌────────┴──────┐ ┌────────┴───────┐                 │
│   │ fund-service  │ │ report-service │                 │
│   │ (资金流水服务) │ │ (报表服务)     │                 │
│   └────────┬──────┘ └────────┬───────┘                 │
├────────────┼─────────────────┼──────────────────────────┤
│            │   基础设施层      │                          │
│   ┌────────┴─────────────────┴──────────┐               │
│   │ Nacos(注册/配置) Redis(缓存)          │              │
│   │ RabbitMQ(消息)   Spring Security+JWT │              │
│   │ Sentinel(限流)   MinIO/OSS(文件)      │              │
│   └────────┬─────────────────┬──────────┘               │
├────────────┼─────────────────┼──────────────────────────┤
│            │      数据层       │                          │
│   ┌────────┴──────┐ ┌────────┴───────┐                 │
│   │  MySQL 8.0+   │ │  Redis 7.x     │                 │
│   │  (主数据库)    │ │  (缓存/分布式锁)│                 │
│   └───────────────┘ └────────────────┘                 │
└─────────────────────────────────────────────────────────┘
```

### 2.2 服务间通信策略

| 场景 | 通信方式 | 说明 |
|------|----------|------|
| 订单服务 → 收付款服务（生成单据） | Feign 同步调用 | 强一致性要求 |
| 收付款审批完成 → 通知 | RabbitMQ 异步 | 解耦通知发送 |
| 资金流水变动 → 报表数据更新 | RabbitMQ 异步 | 非实时场景 |
| 报表大数据量生成 | RabbitMQ + 异步任务 | 避免阻塞 |

---

## 3. 技术选型明细

| 层次 | 技术 | 版本建议 | 选型理由 |
|------|------|----------|----------|
| 开发语言 | Java | 17 LTS | 长期支持版本，性能与生态成熟 |
| 基础框架 | Spring Boot | 3.2.x | 主流微服务脚手架 |
| 微服务治理 | Spring Cloud Alibaba | 2023.0.x | Nacos 注册/配置一体化 |
| ORM | MyBatis-Plus | 3.5.x | 增强型 MyBatis，减少样板代码 |
| 数据库 | MySQL | 8.0+ | 事务支持、窗口函数、JSON 类型 |
| 连接池 | HikariCP | Spring Boot 默认 | 高性能连接池 |
| 缓存 | Redis | 7.x | 热点数据缓存、分布式锁 |
| 消息队列 | RabbitMQ | 3.12 | 异步解耦、审批流通知 |
| 安全框架 | Spring Security + JWT | - | RBAC 权限控制、无状态认证 |
| 接口文档 | Knife4j (Swagger) | 4.x | API 自动文档生成 |
| 对象存储 | MinIO / 阿里云 OSS | - | Excel/PDF 报表文件存储 |
| Excel 处理 | EasyExcel | 3.x | 高性能读写，避免 OOM |
| PDF 生成 | iText / JasperReports | - | 报表 PDF 导出 |
| 日志 | Logback + ELK | - | 审计日志采集与分析 |
| 限流熔断 | Sentinel | 1.8.x | 高并发流量控制 |
| 前端 PC | Vue 3 + Element Plus | - | 主流管理后台方案 |
| 小程序端 | 微信原生框架 | - | 微信生态深度集成 |
| 构建工具 | Maven | 3.9+ | Java 生态标准 |

---

## 4. 模块拆分与职责

### 4.1 项目模块结构

```
FinCoreCms/
├── fin-core-common              # 公共模块
│   ├── common-base              # 基础实体、工具类
│   ├── common-security          # 安全认证、JWT、RBAC
│   └── common-web               # 统一响应、异常处理
├── fin-core-gateway             # API 网关
├── fin-core-order               # 订单结算服务 (order-service)
├── fin-core-payment             # 收付款服务 (payment-service)
├── fin-core-fund                # 资金流水服务 (fund-service)
├── fin-core-report              # 报表服务 (report-service)
├── fin-core-web                 # PC 前端项目
└── fin-core-miniapp             # 小程序项目
```

### 4.2 各服务职责

#### 订单结算服务 (order-service)

- 订单 CRUD、状态流转管理
- 批量费用计算（合同条款匹配、阶梯费率计算）
- 结算明细生成与确认
- 结算单/付款单/收款单生成
- Excel 导入导出及数据校验

#### 收付款服务 (payment-service)

- 收款记录创建、扫码核销、到账确认
- 付款申请、多级审批流程
- 客户账单生成（合并/拆分）
- 账单多渠道发送（邮件、小程序通知）

#### 资金流水服务 (fund-service)

- 银行账户信息维护
- 资金流水手动录入
- 银行接口流水同步（可选对接）
- 自动流水核对与差异标记
- 异常流水提醒处理

#### 报表服务 (report-service)

- 收支明细报表（按时间/账户/订单维度）
- 资金余额报表（实时余额+趋势）
- 结算统计报表（已结算/未结清统计）
- Excel/PDF 格式导出

---

## 5. 数据库设计

### 5.1 订单结算域

#### t_order（订单主表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| order_no | VARCHAR(32) UNIQUE | 订单编号 |
| customer_id | BIGINT | 客户ID |
| customer_name | VARCHAR(100) | 客户名称 |
| total_amount | DECIMAL(18,2) | 订单总金额 |
| settled_amount | DECIMAL(18,2) | 已结算金额 |
| order_status | TINYINT | 状态：1待结算/2已结算/3已关闭 |
| contract_no | VARCHAR(64) | 关联合同号 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除标记 |

索引：`idx_order_no`, `idx_customer_id`, `idx_status`, `idx_create_time`

#### t_order_item（订单明细表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| order_id | BIGINT | 订单ID |
| product_name | VARCHAR(200) | 商品/服务名称 |
| quantity | INT | 数量 |
| unit_price | DECIMAL(18,4) | 单价 |
| amount | DECIMAL(18,2) | 金额 |
| fee_rule_id | BIGINT | 适用费率规则ID |

#### t_settlement（结算单表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| settlement_no | VARCHAR(32) UNIQUE | 结算单号 |
| order_id | BIGINT | 关联订单ID |
| calculated_amount | DECIMAL(18,2) | 计算金额 |
| fee_detail | JSON | 费用明细JSON |
| settlement_status | TINYINT | 状态：0草稿/1已确认/2已作废 |
| settled_by | BIGINT | 结算人ID |
| settled_time | DATETIME | 结算时间 |

#### t_fee_rule（费率规则表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| rule_name | VARCHAR(100) | 规则名称 |
| rule_type | TINYINT | 类型：1合同条款/2阶梯费率 |
| rule_config | JSON | 规则配置JSON |
| effective_date | DATE | 生效日期 |
| expire_date | DATE | 失效日期 |
| is_active | TINYINT | 是否启用 |

### 5.2 收付款域

#### t_receipt（收款记录表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| receipt_no | VARCHAR(32) UNIQUE | 收款单号 |
| payer_name | VARCHAR(100) | 付款方名称 |
| amount | DECIMAL(18,2) | 收款金额 |
| order_id | BIGINT | 关联订单ID |
| bill_id | BIGINT | 关联账单ID |
| receipt_method | TINYINT | 收款方式：1银行转账/2扫码支付/3现金 |
| receipt_status | TINYINT | 状态：1待确认/2已到账/3已核销 |
| confirmed_time | DATETIME | 到账确认时间 |

#### t_payment（付款记录表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| payment_no | VARCHAR(32) UNIQUE | 付款单号 |
| payee_name | VARCHAR(100) | 收款方名称 |
| amount | DECIMAL(18,2) | 付款金额 |
| supplier_id | BIGINT | 供应商ID |
| order_id | BIGINT | 关联订单ID |
| payment_status | TINYINT | 状态：1待审批/2审批中/3已付款/4已驳回 |
| applied_by | BIGINT | 申请人ID |
| applied_time | DATETIME | 申请时间 |

#### t_bill（客户账单表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| bill_no | VARCHAR(32) UNIQUE | 账单编号 |
| customer_id | BIGINT | 客户ID |
| bill_period_start | DATE | 账期开始 |
| bill_period_end | DATE | 账期结束 |
| total_amount | DECIMAL(18,2) | 账单总额 |
| paid_amount | DECIMAL(18,2) | 已付金额 |
| send_status | TINYINT | 发送状态：0未发送/1已发送 |

#### t_approval_flow（审批流程表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| biz_type | VARCHAR(32) | 业务类型：PAYMENT/RECEIPT |
| biz_id | BIGINT | 业务单据ID |
| current_node | INT | 当前审批节点 |
| total_nodes | INT | 总审批节点数 |
| node_config | JSON | 审批节点配置（审批人、顺序） |
| approval_status | TINYINT | 状态：1审批中/2已通过/3已驳回 |
| approved_by | BIGINT | 审批人ID |
| approved_time | DATETIME | 审批时间 |
| comment | VARCHAR(500) | 审批意见 |

### 5.3 资金流水域

#### t_bank_account（银行账户表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| account_no | VARCHAR(32) UNIQUE | 账号（加密存储） |
| bank_name | VARCHAR(100) | 开户行 |
| account_name | VARCHAR(100) | 账户名称 |
| balance | DECIMAL(18,2) | 当前余额 |
| currency | VARCHAR(10) | 币种 |
| account_status | TINYINT | 状态：1正常/2冻结/3注销 |

#### t_fund_transaction（资金流水表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| trans_no | VARCHAR(64) UNIQUE | 流水号 |
| account_id | BIGINT | 账户ID |
| trans_type | TINYINT | 类型：1收入/2支出/3转账 |
| amount | DECIMAL(18,2) | 金额 |
| balance_after | DECIMAL(18,2) | 交易后余额 |
| counterparty | VARCHAR(100) | 对方账户/名称 |
| trans_time | DATETIME | 交易时间 |
| source | TINYINT | 来源：1手动录入/2银行接口 |
| summary | VARCHAR(500) | 摘要 |

#### t_reconciliation（对账记录表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| system_trans_id | BIGINT | 系统流水ID |
| actual_trans_id | VARCHAR(64) | 实际流水号 |
| diff_type | TINYINT | 差异类型：1金额不符/2系统独有/3银行独有 |
| diff_amount | DECIMAL(18,2) | 差异金额 |
| handle_status | TINYINT | 处理状态：0未处理/1已处理/2已忽略 |
| handle_by | BIGINT | 处理人ID |
| handle_time | DATETIME | 处理时间 |
| remark | VARCHAR(500) | 备注 |

### 5.4 通用域

#### t_user（用户表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| username | VARCHAR(50) UNIQUE | 用户名 |
| password | VARCHAR(255) | 加密密码 |
| real_name | VARCHAR(50) | 真实姓名 |
| phone | VARCHAR(20) | 手机号 |
| email | VARCHAR(100) | 邮箱 |
| status | TINYINT | 状态：1启用/2禁用 |

#### t_role（角色表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| role_code | VARCHAR(32) UNIQUE | 角色编码：ADMIN/FINANCE/BIZ |
| role_name | VARCHAR(50) | 角色名称 |
| description | VARCHAR(200) | 描述 |

#### t_user_role（用户角色关联表）

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | BIGINT | 用户ID |
| role_id | BIGINT | 角色ID |

#### t_permission（权限表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| perm_code | VARCHAR(64) UNIQUE | 权限编码 |
| perm_name | VARCHAR(50) | 权限名称 |
| parent_id | BIGINT | 父权限ID（菜单树） |
| perm_type | TINYINT | 类型：1菜单/2按钮/3接口 |

#### t_role_permission（角色权限关联表）

| 字段 | 类型 | 说明 |
|------|------|------|
| role_id | BIGINT | 角色ID |
| perm_id | BIGINT | 权限ID |

#### t_audit_log（审计日志表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| user_id | BIGINT | 操作人ID |
| username | VARCHAR(50) | 操作人用户名 |
| operation | VARCHAR(50) | 操作类型 |
| target_type | VARCHAR(50) | 目标对象类型 |
| target_id | VARCHAR(100) | 目标对象ID |
| request_params | TEXT | 请求参数 |
| response_result | TEXT | 响应结果 |
| client_ip | VARCHAR(50) | 客户端IP |
| create_time | DATETIME | 操作时间 |
| cost_time | BIGINT | 耗时（ms） |

---

## 6. API 接口设计

### 6.1 通用规范

- **Base URL**：`/api/v1`
- **认证方式**：Header `Authorization: Bearer <JWT_TOKEN>`
- **统一响应格式**：

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1718345600000
}
```

- **分页请求参数**：`?page=1&size=20&sort=create_time,desc`
- **分页响应格式**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [],
    "total": 100,
    "page": 1,
    "size": 20,
    "pages": 5
  }
}
```

### 6.2 订单结算服务 `/api/v1/orders`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/orders` | 订单列表（分页+筛选） | FINANCE, BIZ |
| GET | `/orders/{id}` | 订单详情 | FINANCE, BIZ |
| POST | `/orders` | 创建订单 | BIZ, FINANCE |
| PUT | `/orders/{id}` | 更新订单 | BIZ, FINANCE |
| DELETE | `/orders/{id}` | 删除订单（逻辑删除） | ADMIN |
| PUT | `/orders/{id}/status` | 更新订单状态 | FINANCE |
| POST | `/orders/batch-calc` | 批量费用计算 | FINANCE |
| POST | `/orders/import` | Excel 批量导入 | FINANCE |
| GET | `/orders/export` | Excel 批量导出 | FINANCE |

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/settlements` | 结算单列表 | FINANCE |
| GET | `/settlements/{id}` | 结算单详情 | FINANCE |
| POST | `/settlements` | 生成结算单 | FINANCE |
| PUT | `/settlements/{id}/confirm` | 确认结算单 | FINANCE |
| PUT | `/settlements/{id}/cancel` | 作废结算单 | ADMIN |

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/fee-rules` | 费率规则列表 | FINANCE |
| POST | `/fee-rules` | 创建费率规则 | ADMIN |
| PUT | `/fee-rules/{id}` | 更新费率规则 | ADMIN |
| PUT | `/fee-rules/{id}/toggle` | 启用/禁用规则 | ADMIN |

### 6.3 收付款服务 `/api/v1/payments`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/receipts` | 收款记录列表 | FINANCE, ADMIN |
| GET | `/receipts/{id}` | 收款详情 | FINANCE, ADMIN |
| POST | `/receipts` | 创建收款记录 | FINANCE |
| PUT | `/receipts/{id}/confirm` | 到账确认 | FINANCE |
| POST | `/receipts/verify-by-scan` | 扫码核销 | FINANCE |

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/payments` | 付款记录列表 | FINANCE, ADMIN |
| GET | `/payments/{id}` | 付款详情 | FINANCE, ADMIN |
| POST | `/payments` | 发起付款申请 | FINANCE |
| PUT | `/payments/{id}/approve` | 付款审批 | FINANCE, ADMIN |
| PUT | `/payments/{id}/reject` | 驳回付款 | FINANCE, ADMIN |

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/bills` | 账单列表 | FINANCE, BIZ |
| GET | `/bills/{id}` | 账单详情 | FINANCE, BIZ |
| POST | `/bills` | 生成账单 | FINANCE |
| POST | `/bills/merge` | 合并账单 | FINANCE |
| POST | `/bills/split` | 拆分账单 | FINANCE |
| POST | `/bills/{id}/send` | 发送账单 | FINANCE |

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/approvals/pending` | 待审批列表 | FINANCE, ADMIN |
| POST | `/approvals/{id}/process` | 处理审批节点 | FINANCE, ADMIN |

### 6.4 资金流水服务 `/api/v1/funds`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/accounts` | 银行账户列表 | FINANCE, ADMIN |
| GET | `/accounts/{id}` | 账户详情 | FINANCE, ADMIN |
| POST | `/accounts` | 创建银行账户 | ADMIN |
| PUT | `/accounts/{id}` | 更新账户信息 | ADMIN |

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/transactions` | 流水列表（分页+筛选） | FINANCE, ADMIN |
| GET | `/transactions/{id}` | 流水详情 | FINANCE, ADMIN |
| POST | `/transactions` | 手动录入流水 | FINANCE |
| POST | `/transactions/batch-sync` | 银行接口批量同步（可选） | ADMIN |

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/reconciliations` | 对账记录列表 | FINANCE, ADMIN |
| POST | `/reconciliations/run` | 执行自动对账 | FINANCE |
| GET | `/reconciliations/diffs` | 查看差异记录 | FINANCE |
| PUT | `/reconciliations/{id}/handle` | 处理差异记录 | FINANCE |

### 6.5 报表服务 `/api/v1/reports`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/income-expense` | 收支明细报表 | FINANCE, ADMIN |
| GET | `/balance` | 资金余额报表 | FINANCE, ADMIN |
| GET | `/settlement-stats` | 结算统计报表 | FINANCE, ADMIN |
| POST | `/export` | 报表导出（Excel/PDF） | FINANCE, ADMIN |

**报表请求参数示例**：

```json
{
  "report_type": "INCOME_EXPENSE",
  "start_date": "2026-01-01",
  "end_date": "2026-06-30",
  "account_ids": [1, 2],
  "order_dimension": true,
  "export_format": "EXCEL"
}
```

---

## 7. 安全方案设计

### 7.1 认证流程（JWT 无状态认证）

```
┌────────────┐         ┌──────────────┐         ┌────────────┐
│   客户端    │         │  API Gateway  │         │ Auth Service│
└─────┬──────┘         └──────┬───────┘         └─────┬──────┘
      │  POST /login          │                      │
      │  {username, password} │                      │
      │──────────────────────>│                      │
      │                       │  验证凭证              │
      │                       │─────────────────────>│
      │                       │  返回用户+角色+权限    │
      │                       │<─────────────────────│
      │  返回 Access Token    │                      │
      │  (2h) + Refresh Token │                      │
      │<──────────────────────│                      │
      │                       │                      │
      │  后续请求              │                      │
      │  Authorization: Bearer│                      │
      │  <access_token>       │                      │
      │──────────────────────>│                      │
      │                       │  验证签名+有效期       │
      │                       │  解析用户+权限         │
      │                       │  注入请求上下文        │
      │  业务响应              │                      │
      │<──────────────────────│                      │
```

- Token 有效期：Access Token 2小时，Refresh Token 7天
- Token 中存储：`{userId, username, roles[], permissions[]}`
- 登出时将 Token 加入 Redis 黑名单（TTL = Token剩余有效期）

### 7.2 RBAC 三级权限模型

| 角色 | 角色编码 | 核心权限范围 |
|------|----------|-------------|
| **管理员** | ADMIN | 全部功能 + 系统配置（账户管理、费率规则配置、用户管理） |
| **财务专员** | FINANCE | 订单结算、收付款操作、资金流水管理、报表查看 |
| **业务员** | BIZ | 订单录入/查看、账单查询 |

权限控制粒度：
- **菜单级**：前端路由守卫，根据角色动态渲染菜单
- **按钮级**：前端 `v-permission` 指令控制按钮显示
- **接口级**：后端 `@PreAuthorize` 注解校验接口权限

### 7.3 数据安全

| 安全措施 | 实现方式 |
|----------|----------|
| **传输加密** | 全程 HTTPS，TLS 1.3 |
| **密码存储** | BCrypt 加盐哈希 |
| **敏感字段加密** | 账号、金额等使用 AES-256-GCM 加密存储，密钥通过环境变量注入 |
| **SQL 注入防护** | MyBatis-Plus 参数化查询 |
| **XSS 防护** | 前端输出转义 + 后端参数校验 |
| **CSRF 防护** | JWT + SameSite Cookie |
| **日志脱敏** | 审计日志中对手机号、邮箱等字段脱敏处理 |

### 7.4 审计日志

- **记录范围**：所有资金相关操作（创建/修改/删除订单、收款/付款、结算、审批）
- **实现方式**：自定义 `@AuditLog` 注解 + AOP 切面
- **记录信息**：操作人、操作类型、目标对象、请求参数（脱敏）、响应、IP、耗时
- **存储**：`t_audit_log` 表，按月分表归档
- **查询**：提供管理后台审计日志查询页面

---

## 8. 高并发与性能优化

### 8.1 批量结算优化

```
┌────────────────────────────────────────────────────────┐
│                    批量结算流程                          │
├────────────────────────────────────────────────────────┤
│  前端提交订单ID列表 [1,2,3,...,N]                       │
│      │                                                │
│      ▼                                                │
│  Controller 接收请求                                   │
│      │                                                │
│      ▼                                                │
│  Service 分片处理（每批100条）                           │
│      │                                                │
│      ├──> Thread-1: 订单1-100   ──> CompletableFuture  │
│      ├──> Thread-2: 订单101-200 ──> CompletableFuture  │
│      └──> Thread-N: 订单...     ──> CompletableFuture  │
│      │                                                │
│      ▼                                                │
│  CompletableFuture.allOf() 汇总结果                     │
│      │                                                │
│      ▼                                                │
│  返回结算汇总（成功数/失败数/详细结果）                    │
└────────────────────────────────────────────────────────┘
```

- 自定义线程池：核心线程数=CPU核数，最大线程数=CPU核数×2
- 使用 `CountDownLatch` 协调子任务
- 批量结算异步化：提交后返回任务ID，前端轮询进度

### 8.2 缓存策略

| 缓存对象 | TTL | 更新策略 | 存储结构 |
|----------|-----|----------|----------|
| 费率规则 | 30 min | 写时删除缓存 | String (JSON) |
| 银行账户信息 | 30 min | 写时删除缓存 | String (JSON) |
| 用户权限信息 | 60 min | 登录时刷新 | Hash |
| 报表聚合数据 | 10 min | 定时预计算 | String (JSON) |
| 分布式锁（批量结算） | 30 s | 自动过期 | String (SETNX) |

### 8.3 数据库优化

- **分表策略**：订单表、流水表按月分表（`t_order_202601`、`t_order_202602`...），使用 ShardingSphere
- **索引设计**：核心查询字段（`order_no`, `customer_id`, `status`, `create_time`）建立联合索引
- **读写分离**：报表查询走只读从库，写操作走主库
- **连接池**：HikariCP，最大连接数 50

### 8.4 接口限流

使用 Sentinel 对高频接口做 QPS 限流：

| 接口 | QPS 限制 | 策略 |
|------|----------|------|
| `/orders/batch-calc` | 10 QPS | 直接拒绝 |
| `/orders/import` | 5 QPS | 排队等待 |
| `/reports/export` | 5 QPS | 排队等待 |
| 其他查询接口 | 100 QPS | 直接拒绝 |

### 8.5 报表异步生成

大数据量报表采用异步生成模式：

```
请求报表导出
     │
     ▼
生成导出任务 → 返回 taskId
     │
     ▼
RabbitMQ 消息队列
     │
     ▼
异步Worker处理（分页查询 → 写入Excel/PDF → 上传OSS）
     │
     ▼
任务完成 → 推送通知 → 前端下载
```

---

## 9. 开发路线图

### 9.1 总体排期

```
Phase 0: 项目初始化         ████░░░░░░░░░░░░░░░░░░░░░░░░░░░░  第1周
Phase 1: 基础设施搭建        ░░░░████████░░░░░░░░░░░░░░░░░░░░  第2-3周
Phase 2: 订单结算服务        ░░░░░░░░░░░░████████████░░░░░░░░  第4-7周
Phase 3: 收付款服务          ░░░░░░░░░░░░░░░░░░░░░░░████████░░  第8-10周
Phase 4: 资金流水+报表       ░░░░░░░░░░░░░░░░░░░░░░░░░░░░████  第11-13周
Phase 5: 小程序端            ░░░░░░░░░░░░░░░░░░░░░░░░░░░░████  第11-13周(并行)
Phase 6: 集成测试+部署       ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░██  第14-15周
Phase 7: 上线+验收           ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░█  第16周
```

### 9.2 各阶段详细任务

#### Phase 0：项目初始化（第1周）

| 任务 | 产出 | 负责人 |
|------|------|--------|
| Maven 多模块项目搭建 | 项目骨架 + pom.xml | 后端 |
| Vue 3 前端项目初始化 | 前端骨架 + 路由 + 布局 | 前端 |
| 微信小程序项目初始化 | 小程序骨架 + 基础组件 | 小程序 |
| Git 仓库 + CI/CD 流水线 | .gitlab-ci.yml / Jenkinsfile | DevOps |
| 开发规范文档 | 编码规范、Git 提交规范 | Tech Lead |

#### Phase 1：基础设施搭建（第2-3周）

| 任务 | 产出 | 负责人 |
|------|------|--------|
| Spring Cloud 微服务框架搭建 | Gateway + Nacos 注册/配置 | 后端 |
| Spring Security + JWT 认证模块 | 登录/登出/Token刷新 | 后端 |
| RBAC 权限模块 | 用户/角色/权限 CRUD | 后端 |
| 数据库初始化（DDL + 基础数据） | 建表SQL + 初始化脚本 | 后端 |
| MyBatis-Plus 代码生成器 | 基础 Entity/Mapper/Service | 后端 |
| 统一异常处理 + 响应体封装 | common-web 模块 | 后端 |
| Knife4j 接口文档集成 | API 文档自动生成 | 后端 |
| PC 前端登录 + 权限路由 | 登录页 + 动态菜单 | 前端 |
| PC 前端公共组件（表格/表单/弹窗） | 可复用组件库 | 前端 |
| Redis + RabbitMQ 环境搭建 | Docker Compose 配置 | DevOps |

#### Phase 2：订单结算服务开发（第4-7周）

| 任务 | 产出 | 负责人 |
|------|------|--------|
| 订单 CRUD + 状态流转 | order-service 核心接口 | 后端 |
| 费率规则配置管理 | 规则 CRUD + 配置界面 | 后端+前端 |
| 批量费用计算引擎 | 合同条款匹配 + 阶梯费率计算 | 后端 |
| 结算单生成与确认 | 结算流程闭环 | 后端 |
| EasyExcel 导入导出 | 模板下载 + 导入校验 + 批量导出 | 后端 |
| 订单管理页面 | 列表/详情/新建/编辑 | 前端 |
| 费率规则管理页面 | 规则配置表单 | 前端 |
| 结算管理页面 | 批量结算 / 结算单列表 | 前端 |

#### Phase 3：收付款服务开发（第8-10周）

| 任务 | 产出 | 负责人 |
|------|------|--------|
| 收款记录管理 + 到账确认 | receipt CRUD + 状态流转 | 后端 |
| 扫码核销接口 | 小程序扫码 → 后端核销 | 后端 |
| 付款申请 + 多级审批流程 | 审批流引擎 | 后端 |
| 客户账单生成/合并/拆分/发送 | 账单全流程 | 后端 |
| 审批消息推送（RabbitMQ） | 小程序通知 + 邮件 | 后端 |
| 收款管理页面 | 收款列表/详情/核销 | 前端 |
| 付款管理页面 | 付款申请/审批列表 | 前端 |
| 账单管理页面 | 账单生成/发送 | 前端 |
| 审批流页面 | 待审批/已审批/审批详情 | 前端 |

#### Phase 4：资金流水与报表服务（第11-13周）

| 任务 | 产出 | 负责人 |
|------|------|--------|
| 银行账户管理 | 账户 CRUD | 后端 |
| 资金流水手动录入 | 流水录入 + 列表查询 | 后端 |
| 银行接口流水同步（预留） | 抽象接口 + Mock实现 | 后端 |
| 自动流水核对引擎 | 匹配算法 + 差异标记 | 后端 |
| 收支明细报表 | 多维度统计 + 聚合查询 | 后端 |
| 资金余额报表 | 余额趋势 + 变动脉络 | 后端 |
| 结算统计报表 | 结算金额/未结清统计 | 后端 |
| Excel/PDF 异步导出 | RabbitMQ + 异步生成 + OSS | 后端 |
| 账户管理页面 | 账户列表/详情 | 前端 |
| 流水管理页面 | 流水列表/录入 | 前端 |
| 对账管理页面 | 差异列表/处理 | 前端 |
| 报表页面（3个维度） | 图表 + 表格 + 导出按钮 | 前端 |

#### Phase 5：小程序端开发（第11-13周，与 Phase 4 并行）

| 任务 | 产出 | 负责人 |
|------|------|--------|
| 小程序登录 + 授权 | 微信登录对接 | 小程序 |
| 账单查询页面 | 账单列表/详情 | 小程序 |
| 支付确认页面 | 扫码支付/确认 | 小程序 |
| 扫码核销页面 | 扫码 → 核销流程 | 小程序 |
| 审批通知 + 处理 | 消息推送 + 审批操作 | 小程序 |
| 报表摘要页面 | 关键数据卡片展示 | 小程序 |

#### Phase 6：集成测试与优化（第14-15周）

| 任务 | 产出 | 负责人 |
|------|------|--------|
| 前后端联调 | 全流程走通 | 全员 |
| 单元测试（覆盖率 ≥ 70%） | 测试报告 | 后端 |
| 压力测试 | 批量结算 10000 订单 | QA |
| 安全渗透测试 | 渗透测试报告 | QA |
| SQL 性能优化 | 慢查询优化 + 索引调整 | 后端 |
| 前端性能优化 | 打包优化 / 懒加载 | 前端 |
| 部署脚本 + Docker 镜像 | Dockerfile + K8s yaml | DevOps |

#### Phase 7：上线部署与验收（第16周）

| 任务 | 产出 | 负责人 |
|------|------|--------|
| 生产环境部署 | 上线清单 | DevOps |
| 用户培训 | 操作手册 + 培训视频 | Tech Lead |
| 验收文档交付 | 技术文档 + API文档 + 运维手册 | Tech Lead |

### 9.3 里程碑节点

| 里程碑 | 时间节点 | 交付物 |
|--------|----------|--------|
| M1 - 基础框架就绪 | 第3周末 | 项目可运行，登录+权限可用 |
| M2 - 订单结算功能完成 | 第7周末 | 订单管理+批量结算全流程 |
| M3 - 收付款功能完成 | 第10周末 | 收款+付款+审批全流程 |
| M4 - 资金+报表+小程序完成 | 第13周末 | 全部功能就绪 |
| M5 - 测试通过 | 第15周末 | 测试报告通过 |
| M6 - 正式上线 | 第16周末 | 生产环境运行 |

---

## 10. 风险评估与应对

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 财务计算精度问题（浮点数误差） | 高 | 中 | 全部金额使用 `BigDecimal` 计算，单元测试覆盖边界值 |
| 银行接口对接延期 | 中 | 高 | 第一期仅支持手动录入，预留银行接口抽象层，后续迭代补充 |
| 审批流复杂度超预期 | 中 | 中 | 使用轻量级状态机实现，避免引入重量级工作流引擎 |
| 小程序审核周期长 | 低 | 中 | 提前提交审核，准备完善的隐私政策和测试账号 |
| 大数据量报表性能瓶颈 | 中 | 中 | 异步生成 + 分页查询 + 数据预聚合，必要时使用 OLAP |
| 安全性合规不达标 | 高 | 低 | 开发阶段嵌入安全测试，编码规范强制执行（SonarQube扫描） |
| 团队对技术栈不熟悉 | 中 | 低 | 前两周进行技术分享和 Code Review 规范建立 |

---

## 11. 验收标准

### 11.1 功能验收

- [ ] 订单 CRUD + 状态流转正常
- [ ] 批量费用计算准确（与手工计算结果一致，误差 < 0.01 元）
- [ ] 结算单/付款单/收款单生成正确
- [ ] Excel 导入导出无数据丢失，校验规则生效
- [ ] 收款记录、扫码核销功能正常
- [ ] 付款多级审批流程完整（驳回/通过/转审）
- [ ] 客户账单生成/合并/拆分/发送正常
- [ ] 资金流水录入、同步、对账差异标记正常
- [ ] 三张基础报表数据准确
- [ ] 报表 Excel/PDF 导出完整
- [ ] 小程序端所有功能可用

### 11.2 性能验收

- [ ] 单接口查询响应 ≤ 3 秒（P99）
- [ ] 批量结算 10000 条订单 ≤ 5 分钟
- [ ] 并发 200 用户同时操作，系统不崩溃
- [ ] 报表导出 10000 行数据 ≤ 30 秒

### 11.3 安全验收

- [ ] 所有资金接口需要认证
- [ ] RBAC 权限隔离生效（越权操作返回 403）
- [ ] 敏感字段加密存储（数据库直查为密文）
- [ ] 审计日志完整记录所有资金操作
- [ ] 通过基础安全渗透测试（SQL注入、XSS、CSRF）

### 11.4 交付物验收

- [ ] 完整技术文档（架构设计、数据库设计、接口文档、部署文档）
- [ ] 用户操作手册（PC端 + 小程序端）
- [ ] 压力测试报告
- [ ] 安全审计报告
- [ ] 源代码 + 构建脚本 + Docker 镜像

---

> **文档说明**：本开发方案基于《财务管理模块开发需求文档》V1.1 分析生成，方案中的排期和资源评估为建议值，实际执行需根据团队规模和能力调整。需求变更时需同步更新本方案。

> **编制单位**：[XXX公司]  
> **编制日期**：2026-06-14
